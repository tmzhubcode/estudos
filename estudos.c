#include <stdio.h>
int main(){

    int a = -2 , b = -9;
    if (a>0 && b>0)
    {
    printf(" os dois numeros são positivos \n");
    } else{
        printf("pelo menos um dos numeros são positivos");
    }
    
}
